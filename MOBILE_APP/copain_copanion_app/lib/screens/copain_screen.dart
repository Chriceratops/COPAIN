import 'package:flutter/material.dart';

class CopainScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    
    return Scaffold(
      appBar: AppBar(title: Text("copain_screen")),
    );
  }
  
}